#include "main.h"
#include <sys/types.h>
#include <sys/stat.h>
#include <unistd.h>
int check_file(char *path, char *filepath)
{
	int flag = 0, i = 0;
	char **path_arr = token_parse(path, ":"), *file_path;
	struct stat st;

	if (stat())
	while (path_arr[i])
	{
		*file_path
	}
}
